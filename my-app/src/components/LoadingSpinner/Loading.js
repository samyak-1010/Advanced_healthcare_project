import React from 'react';
import './Loading.css'; // Create a separate CSS file or use inline styles

const Spinner = () => {
  return (
    <div className="spinner"></div>
  );
};

export default Spinner;
